"""Training scaffold: losses, optimizers, and single-batch step functions.

Milestone 3A: minimal mechanics only (loss, optimizer, one train/eval step) to
verify the backward/update loop works on a tiny batch. No full training loop,
scheduler, checkpointing, or evaluation harness lives here yet.
"""
